import { Ux } from '@salesforce/sf-plugins-core';
import { ResultFormatter, ResultFormatterOptions } from '../resultFormatter.js';
type StatusActualState = 'Deleted' | 'Add' | 'Changed' | 'Unchanged';
export type StatusOrigin = 'Local' | 'Remote';
export type StatusStateString = `${StatusOrigin} ${StatusActualState}` | `${StatusOrigin} ${StatusActualState} (Conflict)`;
export type StatusResult = {
    state: StatusStateString;
    fullName: string;
    type: string;
    filePath?: string;
    ignored?: boolean;
    conflict?: boolean;
    actualState?: StatusActualState;
    origin: StatusOrigin;
};
export declare const exitCodeAsNumber: () => number | undefined;
export declare class StatusFormatter extends ResultFormatter {
    private statusRows;
    constructor(ux: Ux, options: ResultFormatterOptions, statusRows: StatusResult[]);
    getJson(): StatusResult[];
    display(): void;
}
export {};
